# from db.database import engine, Base
# from models.models import User, Workspace, WorkspaceSettings, WorkspaceMember, KnowledgeBase, KnowledgeFile, APIKey

# # Create tables
# Base.metadata.create_all(bind=engine)
